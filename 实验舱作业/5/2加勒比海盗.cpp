#include <bits/stdc++.h>

using namespace std;
int n, c;
int a[10005];
int sum = 0, cnt = 0;
int main() {
    cin >> c >> n;
    for(int i = 0; i < n; i++) cin >> a[i];
    sort(a, a + n);
    for(int i = 0; i < n; i++) {
        cnt += a[i];
        if(cnt > )
    }
    return 0;
}