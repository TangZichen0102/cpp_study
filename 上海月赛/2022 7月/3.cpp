#include <bits/stdc++.h>

using namespace std;
int a[100005];
int n;
int sum;
int main() {
    while(cin >> a[n]) n++;
    sort(a, a + n);
    for(int i = n - 1; i >= 0; i--) {
        for(int j = 0; j < n; j++) if()
    }
    return 0;
}