#include <bits/stdc++.h>

using namespace std;
int n;
int vis[11];
int main() {
    cin >> n;
    for(int i = 1; i <= n; i++) {
        int d = n - i;
        if(d == 0) break;
        for(int j = 0; ; j++) {
            string s1 = to_string(j);
            string s2 = to_string(j * d)
        }
    }
    return 0;
}