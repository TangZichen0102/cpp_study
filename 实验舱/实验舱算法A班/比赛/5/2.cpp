#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    for(int i = 0; i < n + m; i++) {
        int a;
        cin >> a;
    }
    cout << -1;
    return 0;
}