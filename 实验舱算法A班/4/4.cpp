#include <bits/stdc++.h>

using namespace std;
int n, a[100005];
int b[100005], c[100005];
int main() {
    cin >> n;
    for(int i = 1; i <= n; i++) cin >> a[i], c[i] = a[i];
    for(int i = 1; i <= n; i++) cin >> b[i];
    int pos = N + 1;
    return 0;
}